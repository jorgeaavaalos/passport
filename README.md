# Passport.js implementation using Node.js

This repository uses express-session to store the user's data, MySQL to store the session on the backend and Passport's local strategy to authenticate users.
